import React from "react";
import MenuIcon from "@mui/icons-material/Menu";

export const Header = () => {
  return (
    <div
      style={{
        backgroundColor: "#fff",
        padding: " 10px 20px",
      }}
    >
      <MenuIcon style={{ color: "#4060AF" }} />
    </div>
  );
};
